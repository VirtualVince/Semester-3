// We can have a Dunk in a Pond class, but these objects are separate in memory
public class Duck
{
    public int BeakLength;
    
    public Duck(int beakLength)
    {
        BeakLength = beakLength;
    }
}